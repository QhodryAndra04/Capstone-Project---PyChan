<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?android:attr/windowBackground"
    android:orientation="vertical"
    android:padding="16dp"> <!-- Background mengikuti tema saat ini -->

    <!-- Title: Settings -->
    <TextView
        android:id="@+id/settingsTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginBottom="45dp"
        android:fontFamily="@font/roboto_medium"
        android:text="Settings"
        android:textColor="@color/green"
        android:textSize="24sp" />

    <!-- Option 2: Change Password Text (TextView) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="20dp"
        android:orientation="horizontal"
        android:padding="8dp">

        <ImageView
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/ic_password" />

        <TextView
            android:id="@+id/tvChangePasswordText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:text="Change Password"
            android:textColor="?android:attr/textColorPrimary" /> <!-- Teks mengikuti tema -->
    </LinearLayout>

    <!-- Option 3: Repository GitHub -->
    <LinearLayout
        android:id="@+id/menuGithub"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="8dp">

        <ImageView
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/ic_github" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:text="Repository GitHub"
            android:textColor="?android:attr/textColorPrimary" /> <!-- Teks mengikuti tema -->
    </LinearLayout>

</LinearLayout>
